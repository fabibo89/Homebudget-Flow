# Kopieren nach Repository-Root als .env und Werte eintragen (oder nach devtools/.env).
# Datei nicht committen (.gitignore). Empfohlen: chmod 600 .env

FINTS_USER=
FINTS_PIN=
FINTS_PRODUCT_ID=

# Optional (Comdirect-Defaults passen meist):
# FINTS_BLZ=20041111
# FINTS_ENDPOINT=https://fints.comdirect.de/fints
